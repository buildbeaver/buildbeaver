export enum Status {
  Running = 'running',
  Canceled = 'canceled',
  Failed = 'failed',
  Queued = 'queued',
  SkippedJob = 'skipped-job',
  SkippedStep = 'skipped-step',
  Submitted = 'submitted',
  Succeeded = 'succeeded',
  Unknown = 'unknown'
}
